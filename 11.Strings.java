//These are string methods, there are many that can be accesed by a dot next to a string

name.isBlank();     //Check if the string is ""
name.inEmpty();     //Check if the string is ""
name.equals();      //checks if the string is equal to the value in parenthesis     BOOLEAN
name.length();      //Checks the length of the string or string[]                   INTEGER
name.charAt(0);     //Returns the character in the given index                      CHAR
name.indexOf("y");  //Returns the index of the given char                           INTEGER
name.toUpperCase();
name.toLowerCase();
name.trim();        //Gets rid of empty spaces
name.replace(x,y);