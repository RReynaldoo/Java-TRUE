public class Name {
	public static void main(String[]args)
	{
		double x = 3.14;
		double y = -12;
		
		//These are some useful methods of math class, there are plenty more
		Math.abs(x);
		Math.max(x, y);
		Math.min(x, y);
		Math.sqrt(x);
		Math.round(x);
		Math.ceil(x);
		Math.floor(x);
	}

}
